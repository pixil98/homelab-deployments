apiVersion: v1
kind: Service
metadata:
  name: ocis-antivirus
  namespace: cloud
  labels:
    ocis-metrics: enabled
    app.kubernetes.io/name: antivirus
    app.kubernetes.io/instance: ocis
spec:
  selector:
    app.kubernetes.io/name: antivirus
    app.kubernetes.io/instance: ocis
  type: ClusterIP
  ports:
    - name: metrics-debug
      port: 9277
      protocol: TCP
