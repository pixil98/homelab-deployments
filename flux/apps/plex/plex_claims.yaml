apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-pvc
  namespace: plex
spec:
  storageClassName: nfs-client-backup
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
