apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: necesse
  namespace: games
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
