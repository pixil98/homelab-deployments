apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentik
  namespace: auth
  labels:
    app.kubernetes.io/name: authentik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - "auth.dev.lab.reisman.org"
      secretName: authentik-tls
  rules:
    #TODO: update hostname
    - host: "auth.dev.lab.reisman.org"
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: authentik
                port:
                  name: http